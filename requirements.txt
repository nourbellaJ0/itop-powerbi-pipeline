# ============================================================
# requirements.txt — Dépendances du pipeline iTop → Power BI
#
# Installation :
#   pip install -r requirements.txt
#
# Python requis : 3.10 ou supérieur (utilisation de X | Y pour les unions de types)
# ============================================================

# ── Core HTTP / API ───────────────────────────────────────────────────────────
requests>=2.31.0            # Appels HTTP vers l'API iTop et Microsoft Graph
urllib3>=2.0.0              # Stratégie de retry HTTP (utilisée par requests)

# ── Data processing ───────────────────────────────────────────────────────────
pandas>=2.1.0               # Transformation et nettoyage des données
openpyxl>=3.1.2             # Export Excel (.xlsx) — moteur requis par pandas

# ── Configuration / Secrets ───────────────────────────────────────────────────
python-dotenv>=1.0.0        # Chargement des variables depuis le fichier .env

# ── SQL (requis si OUTPUT_MODE=sql) ───────────────────────────────────────────
SQLAlchemy>=2.0.0           # ORM et abstraction SQL

# Décommenter le driver adapté à votre base de données :
# pyodbc>=5.0.0             # SQL Server via ODBC (Windows/Linux)
# psycopg2-binary>=2.9.0   # PostgreSQL (binaire, pas de compilation)
# psycopg2>=2.9.0          # PostgreSQL (à compiler, recommandé en production)

# ── Tests ─────────────────────────────────────────────────────────────────────
pytest>=8.0.0               # Exécution des tests unitaires (python -m pytest tests/)

# ── Notes d'installation par plateforme ───────────────────────────────────────
# Windows — SQL Server :
#   1. pip install pyodbc
#   2. Installer "ODBC Driver 17 for SQL Server" depuis Microsoft
#      https://learn.microsoft.com/fr-fr/sql/connect/odbc/download-odbc-driver-for-sql-server
#
# Linux — SQL Server :
#   1. Installer le driver ODBC Microsoft pour Linux (voir docs Microsoft)
#   2. pip install pyodbc
#
# PostgreSQL (toutes plateformes) :
#   pip install psycopg2-binary
